# Copy this file to `.env` and adjust values for your machine.
# NEVER commit the real `.env` — it is gitignored.

PORT=4001
JWT_SECRET=dev-secret-change-me
CORS_ORIGIN=http://localhost:5174
PGHOST=127.0.0.1
PGPORT=5433
PGUSER=jewelry
PGPASSWORD=jewelry123
PGDATABASE=jewelry
UPLOAD_DIR=uploads
